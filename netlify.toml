[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev:frontend"
  port = 3000
  targetPort = 5173

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
